{ "aaData": [
<%
  first = true
  @admin_request_listing.reserves.each do  | reserve |
    reserve = AdminReserveRow.new(reserve)
    if !first
%>, <% end %><% cache (reserve.cache_key) do %>
  <%= raw reserve.to_json %>
<%
    end
    first = false
  end
%>
] }
