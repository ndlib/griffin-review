{ "aaData": [
<%
  request_rows = @admin_request_listing.reserves.collect {|reserve| RequestRow.new(reserve)}
  request_rows.each_with_index do |row, index|
%><%= index > 0 ? ',' : '' %>
  <%= row.cached_json %><% end %>
] }
