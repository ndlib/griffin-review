{ "aaData": [
<%
  @admin_request_listing.reserves.each_with_index do  | reserve, index |
    reserve = RequestRow.new(reserve)
    if index == 0
%>, <% end %>
  <%= raw reserve.cached_json %>
  <%
  end
%>
] }
