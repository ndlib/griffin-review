<h5>URL</h5>
<%= f.input :url, :label => false, :hint => "Be sure to include the http://" %>

<div class="form-actions">
  <%= f.button :submit, "Save", :class => 'btn btn-primary' %>

  <%= link_to 'Cancel', request_path(@request.id) %>
</div>
