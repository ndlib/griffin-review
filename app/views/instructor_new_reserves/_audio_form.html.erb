
<h3>2. Audio Request </h3>

<%= simple_form_for object, :as => :video, :html => {id: "audio_form"}, :url => course_reserves_path() do |f| %>
  <%= f.input :journal_title, label: 'Album' %>
  <%= f.input :title %>
  <%= f.input :creator %>

  <%= f.input :length, label: "Tracks / Clips" do %>
    <%= f.input_field :length %>
  <% end %>

  <%= f.input :needed_by do %>
    <%= f.input_field :needed_by, as: "string", class: 'datepicker span2' %>
  <% end %>

  <%= f.input :note, label: "Special Instuctions" do %>
    <%= f.input_field :note, as: "text", placeholder: "Other notes to identify the work. ", rows: 5 %>
  <% end %>

  <%= f.input :library, as: "hidden", value: 'Hesburgh' %>

  <div class="form-actions">
    <%= f.button :submit, "Save", :class => 'btn-primary' %>
    <%= link_to 'Cancel', course_path(@course.id), :class => "btn btn-danger" %>
  </div>
<% end %>
