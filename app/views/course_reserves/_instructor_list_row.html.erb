<% row = InstructorCourseRow.new(object, controller) %>
<tr class="reserve_<%= object.id %>">
  <td>
    <%= render :partial => 'list_record', :locals => { object: row.reserve} %>
  </td>
  <!-- td class="topic_cell">
    <% # render partial: '/topics/topic_edit_link', locals: { object: object } %>
    <% # render partial: '/topics/add_topics_form', locals: { reserve: object } %>
  </td -->
  <td><%= row.workflow_state %></td>
  <td><p class="text-center"><%= row.edit_link %></p></td>
  <td><p class="text-center"><%= row.delete_link %></p></td>
  <td><%= row.delete_sort %></td>
  <td><%= row.sortable_title %></td>
</tr>
