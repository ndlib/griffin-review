<% reserve_header = ReserveHeader.new(reserve) %>

<div class="page-header">
  <h1><%= reserve_header.title %> </h1>
  <p class="lead">
    <%= raw reserve_header.creator_contributor %>
    <small><%= raw reserve_header.details %></small>
    <small><%= raw reserve_header.publisher_provider %></small>
  </p>
</div>
